'use strict';

//------------------------------------------------------------------------------
// Rule Definition
//------------------------------------------------------------------------------

module.exports = function(context) {

  // return {
  //   // 'Program': function(node) {
  //   //   context.report(node, 'Illegal import');
  //   // },
  //
  //   // var name = node.name.replace(/^_+|_+$/g, ""),
  //
  //   'Identifier': function(node) {
  //     context.report(node, 'Illegal iiiidentifier', {name: node.name});
  //   },
  //   'FunctionDeclaration': function(node) {
  //     context.report(node, 'Illegal contract syntax');
  //   },
  //   'FunctionDeclaration:exit': function(node) {
  //     context.report(node, 'Illegal contract syntax exit');
  //   }
  // };
};
